import { StyleSheet } from 'react-native';
import { theme } from '@/constants/theme';

// Shared by the Reports screen and its SavingsRing.
export const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: theme.colors.background },
  centerBox: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 32, gap: 8 },
  errorTitle: { fontFamily: theme.font.display, fontSize: 16, color: theme.colors.expense },
  errorDetail: { fontSize: 12.5, color: theme.colors.textSecondary, textAlign: 'center', lineHeight: 18 },
  emptyBanner: {
    marginHorizontal: 20,
    marginBottom: 12,
    padding: 12,
    borderRadius: theme.radius.md,
    backgroundColor: theme.colors.surfaceAlt,
    borderWidth: theme.border.thin,
    borderColor: theme.colors.ink,
  },
  emptyBannerText: { fontSize: 12.5, color: theme.colors.textSecondary, lineHeight: 18 },

  heroCard: { marginHorizontal: 20, padding: 18 },
  heroTop: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start' },
  heroLabel: {
    fontSize: 10.5,
    fontFamily: theme.font.bodyBold,
    letterSpacing: 0.5,
    color: theme.colors.textMuted,
    marginTop: 6,
  },
  heroValue: { fontFamily: theme.font.monoBold, fontSize: 28, marginTop: 2 },
  heroSub: {
    fontSize: 11.5,
    fontFamily: theme.font.bodyMedium,
    color: theme.colors.textSecondary,
    marginTop: 2,
  },
  ringTextWrap: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    alignItems: 'center',
    justifyContent: 'center',
  },
  ringText: { fontSize: 9.5, fontFamily: theme.font.bodyBold, color: theme.colors.textPrimary },
  heroSplit: {
    flexDirection: 'row',
    alignItems: 'stretch',
    marginTop: 16,
    paddingTop: 14,
    borderTopWidth: StyleSheet.hairlineWidth,
    borderTopColor: theme.colors.border,
  },
  splitItem: { flex: 1 },
  splitLabelRow: { flexDirection: 'row', alignItems: 'center', gap: 6, marginBottom: 4 },
  splitDot: { width: 7, height: 7, borderRadius: 4 },
  splitLabel: { fontSize: 11, fontFamily: theme.font.bodyBold, color: theme.colors.textSecondary },
  splitValue: { fontSize: 15, fontFamily: theme.font.bodyBold },
  splitTrend: { fontSize: 10, color: theme.colors.textMuted, marginTop: 2 },
  splitDivider: {
    width: StyleSheet.hairlineWidth,
    backgroundColor: theme.colors.border,
    marginHorizontal: 16,
  },

  savingsRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginHorizontal: 20,
    marginTop: 12,
    padding: 14,
    borderRadius: 12,
    backgroundColor: theme.colors.surfaceAlt,
  },
  savingsLabel: { fontSize: 13, color: theme.colors.textSecondary },
  savingsHint: { fontSize: 10.5, color: theme.colors.textMuted, marginTop: 2 },
  savingsValue: { fontSize: 14, fontFamily: theme.font.bodyBold, color: theme.colors.gold },
  emptyText: { marginHorizontal: 20, color: theme.colors.textMuted, fontSize: 13 },
  chartToggleWrap: { paddingHorizontal: 20, marginTop: 4, marginBottom: 4 },
  chartCard: { marginHorizontal: 20, marginBottom: 4, padding: 16 },
  pieView: { flexDirection: 'row', alignItems: 'center', gap: 14 },
  pieLegend: { flex: 1, minWidth: 0, gap: 8 },
  pieLegendRow: { flexDirection: 'row', alignItems: 'center' },
  pieLegendName: {
    flex: 1,
    fontSize: 12,
    fontFamily: theme.font.bodyMedium,
    color: theme.colors.textPrimary,
    marginRight: 6,
  },
  pieLegendPct: { fontSize: 12, fontFamily: theme.font.bodyBold, color: theme.colors.textSecondary },
  legendRow: { flexDirection: 'row', gap: 16, marginBottom: 6 },
  legendItem: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  legendDot: { width: 9, height: 9, borderRadius: 5 },
  legendLabel: { fontSize: 12, color: theme.colors.textSecondary, fontFamily: theme.font.bodyMedium },
  trendTotal: { fontFamily: theme.font.bodyBold, fontSize: 20, color: theme.colors.textPrimary },
  trendSub: {
    fontFamily: theme.font.body,
    fontSize: 11,
    color: theme.colors.textMuted,
    marginTop: 2,
    marginBottom: 8,
  },
  dot: { width: 9, height: 9, borderRadius: 5, marginRight: 8 },
});
